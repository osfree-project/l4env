# directories we need to know
PKGDIR		?= ..
L4DIR		?= $(PKGDIR)/../..

# no targets, we only use well known subdirs
TARGET = include audio video de-muxer support fx predict h264_schedule

# include subdir role
include $(L4DIR)/mk/subdir.mk
